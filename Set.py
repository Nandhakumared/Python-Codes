'''Set  '''
# SET:
#     It is collection elements/values. It contains only unique values.
#     It is a collection of unique values.
#     Here we use {}
#     If we print set values it will display the values in random order not in sequence.
#     In set if we print values, in output it will skip duplicate values.

place = {25, 68, 31, 87, 53, 75}

print(place)